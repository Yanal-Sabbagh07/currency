import React from "react";

const Button = (props) => {
  const handleClick = (e) => {};
  return (
    <button type="submit" className="convert-container" onClick={handleClick}>
      Convert
    </button>
  );
};

export default Button;
